`zsmith.jar` is committed here for convenience so jfrdoc runs out of the box.

Built from https://github.com/AdamBien/zsmith
- version: `2026.05.12.01`
- commit: `135f81a`

To rebuild from source, clone zsmith and run `./zb.sh`, then copy
`zbo/zsmith.jar` over the file in this directory.
